# Netlify configuration for Next.js (App Router)
# Uses Netlify's official Next.js runtime plugin to run SSR + API routes.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"